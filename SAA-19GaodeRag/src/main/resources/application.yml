server:
  port: 8019

spring:
  application:
    name: SAA-19GaodeRag

  ai:
    dashscope:
      api-key: ${aliQwen-api}
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      chat:
        options:
          model: qwen-plus
    weather:
      amap:
        key: ${AMAP_API_KEY}

logging:
  level:
    com.atguigu.study: DEBUG
    org.springframework.ai: INFO

